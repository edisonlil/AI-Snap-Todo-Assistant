# AI Snap Todo Assistant 方案评估与路线图

## 1. 结论

推荐路线：

- 保留现有 `Python + PyQt6` 截图能力层
- 先补齐 `Todo / Timeline / Attachment` 任务内核
- UI 分两步走：先用当前 Widgets 跑通可用版，再逐步迁移 `Todo` 主视觉界面到 `QML`

这样做的核心原因：

- 现有截图工具已经相对稳定，不应该为了 UI 一起推倒
- 下一阶段真正缺的是任务闭环，不是截图能力
- 如果一直停留在 Widgets，能做整洁 UI，但高级动效成本会越来越高
- `QML` 是当前项目里提升 UI 质感的最低重构成本方案

## 2. 当前实现评估

### 2.1 现有能力

- 全局热键、截图 overlay、多屏和 DPI 处理已经具备
- 截图后编辑、连续截图、多图分析已经具备
- AI 调用、反馈保存、Prompt 管理已经具备

### 2.2 当前短板

- 当前主流程仍然是“截图 -> AI 分析 -> 结果编辑 -> 复制 -> 清空”
- 没有真正的 `Todo` 实体和时间线模型
- 没有任务级持久化和任务状态管理
- UI 还是单次结果流，不是长期常驻任务流
- 若继续在 Widgets 上堆高级视觉效果，维护成本会持续上升

## 3. 方案判断

### 3.1 业务方案是否可行

可行，而且方向正确。

PRD 的核心闭环是：

- 未选中待办时，截图默认创建新 Todo
- 已选中待办时，截图默认追加到当前 Todo
- AI 负责提取结构化信息和时间线摘要
- Todo 仅在完成后消失
- 右上角 Todo 栏在存在未完成任务时常驻

这套交互逻辑是清晰且低认知负担的，适合一线支持、售后、集成工程师这类高频切换场景。

### 3.2 当前技术路线是否要重做

不建议整体推倒重做。

建议：

- 保留 Python 作为系统能力层
- 保留现有截图链路
- 重构业务层
- 后续升级 UI 层

### 3.3 纯 Python 是否会很吃力

要分两部分看：

- 业务和系统能力：不吃力，Python 非常适合
- 高级精美 UI：如果长期只用 `PyQt6 Widgets`，会越来越吃力

结论：

- `Python` 不是问题
- “全靠 Widgets 做未来主界面”才是问题

## 4. UI 与交互评估

### 4.1 当前路线对交互是否友好

友好，尤其是以下几点：

- Todo 常驻右上角，便于用户保持上下文
- 半透明浮层可降低对工作界面的压迫感
- 选中 Todo 后下一次截图直接追加，减少中断
- 追加完成自动取消选中，可降低误挂风险
- 完成后才消失，符合任务追踪心智

### 4.2 是否支持平滑、果冻感、柔性动画

支持，但分两档：

#### Widgets 可支持

- 淡入淡出
- 轻微缩放
- hover 高亮
- 卡片新增/删除基础过渡
- 面板滑入滑出

#### QML 更适合支持

- 果冻感回弹
- 柔性压缩/释放
- 连续形变
- 吸附和跟手
- 高级卡片状态切换
- 时间线节点动效

结论：

- 当前路线适合先把交互逻辑做对
- 如果目标是高质感动态界面，后续应迁移主 UI 到 `QML`

## 5. 为什么推荐 QML

`QML` 对当前项目属于“低成本提升 UI 上限”的方案。

原因：

- 仍在 Qt 体系内，不需要重写系统能力
- Python 截图、热键、AI、存储逻辑都能保留
- 只替换 UI 表现层
- 比 Electron/Tauri 的整体改造成本低很多
- 对动画、状态、层次、透明度、卡片交互更友好

### 5.1 为什么不建议现在切 Electron / Tauri

- 截图 overlay、多屏、置顶、系统热键等桥接复杂度更高
- 当前项目最难的部分不是 UI，而是桌面系统能力
- 现阶段投入产出比不高

## 6. 跨平台策略

### 6.1 当前结论

跨平台很重要，但当前阶段不实现 macOS 兼容。

策略是：

- 现在不做 macOS 适配
- 但从这一阶段开始预留跨平台边界
- 避免继续把 Windows 平台细节直接写死在主流程和业务模块中

### 6.2 成本判断

如果按推荐架构推进：

- `Todo / Timeline / AI / 存储 / QML UI` 这部分未来迁到 macOS 的成本是低到中等
- `全局热键 / 截图 overlay / 单实例 / 置顶窗口 / 打包签名` 这部分未来迁到 macOS 的成本是中等

如果当前继续把 Windows 逻辑直接写进主流程：

- 未来 macOS 兼容成本会显著上升

### 6.3 当前阶段必须遵守的跨平台预埋原则

- 不在 `main.py` 中继续扩散 Windows 专属逻辑
- 平台相关能力统一收敛到独立适配层
- `Todo`、时间线、存储、AI 调用、同步协议必须保持平台无关
- UI 层尽量不依赖平台特有行为
- 后续新增系统能力时，优先设计接口，再做 Windows 实现

### 6.4 建议的预留目录方向

后续可逐步引入：

- `platform/hotkey/`
- `platform/overlay/`
- `platform/single_instance/`
- `platform/windowing/`

当前先保留现有实现，但新代码尽量朝这个方向收敛。

## 7. 推荐架构

建议后续逐步拆为四层：

### 6.1 capture_core

负责：

- 全局热键
- 截图 overlay
- 标注
- 导出截图

### 6.2 todo_domain

负责：

- Todo
- Timeline
- Attachment
- 当前选中态
- 完成状态
- 追加逻辑

### 6.3 services

负责：

- AI 分析
- OCR
- 附件分析
- 本地持久化
- 外部同步

### 6.4 ui_shell

负责：

- 右上角 Todo 栏
- 详情侧栏
- 结果确认
- 动效与视觉表现

### 7.1 平台适配层补充

在四层之外，后续需要增加 `platform` 适配层，专门承接：

- Windows 热键实现
- Windows 截图实现
- Windows 单实例实现
- 后续 macOS 对应实现

目标不是现在实现跨平台，而是现在开始避免架构被 Windows 绑定死。

## 8. 路线图

### 第一阶段：补任务内核

目标：

- 建立 Todo、Timeline、Attachment 的数据模型
- 建立本地持久化
- 跑通“未选中创建 / 已选中追加 / 完成消失”
- 不影响现有截图功能

当前分支 `feature/todo-panel-v1` 应先收敛到这个目标。

补充要求：

- 新能力实现时避免继续把平台细节写死
- 优先抽业务模型和 UI 模块边界

### 第二阶段：补详情工作流

目标：

- 增加 Todo 详情侧栏
- 展示 Timeline
- 展示 AI 摘要
- 支持编辑标题、摘要、状态
- 为附件入口预留位置

### 第三阶段：开始 UI 升级

目标：

- 优先把右上角 Todo 栏迁移到 `QML`
- 再迁移详情侧栏
- 实现更平滑的出现/消失动画
- 做卡片选中态、hover 态、完成态过渡

### 第四阶段：多模态与同步

目标：

- 日志文件分析
- 配置截图分析
- 视频/录屏延后
- 同步到 ACH / 表格系统

### 第五阶段：体验打磨

目标：

- 果冻感回弹
- 柔性卡片交互
- 时间线动画
- 更统一的 loading / empty / error 状态

### 第六阶段：评估跨平台落地

这一阶段才开始正式评估和落地 macOS：

- 替换或补齐平台适配层
- 评估 macOS 截图和热键方案
- 处理打包、签名、权限申请

注意：

- 这不是当前阶段目标
- 当前阶段只做“预埋兼容能力”，不做实际兼容实现

## 9. 当前分支实施原则

在 `feature/todo-panel-v1` 分支继续推进时，遵循以下原则：

- 不重写现有截图核心
- 不把业务闭环和大规模 UI 重做放在同一阶段
- 优先模块化，避免继续把状态堆在 `main.py`
- 所有新能力优先以“旁路接入”的方式落地
- 新 UI 必须不干扰截图主流程
- 当前不实现 macOS，但新模块尽量不要继续加深 Windows 绑定

## 10. 最终建议

最终建议一句话概括：

先用当前技术栈把产品闭环做对，再把主视觉界面迁移到 `QML` 做精美和高级动效。

这条路线兼顾：

- 稳定性
- 迭代速度
- 风险控制
- 长期 UI 上限
