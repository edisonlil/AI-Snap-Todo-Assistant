# AICA UI 设计规范

本文档用于约束 AICA 后续 UI 的视觉方向、组件风格和交互尺度，避免新增页面逐步跑偏。

## 适用范围

- 优先适用于当前 QML 新界面：
  - `src/aica/qml/ControlPanel.qml`
  - `src/aica/qml/ResultDialog.qml`
  - `src/aica/qml/TodoDetailPanel.qml`
  - `src/aica/qml/TodoPanel.qml`
  - `src/aica/qml/FocusHintDialog.qml`
- `src/aica/toolbar.py` 属于截图操作工具栏，因叠加在真实截图上，需要更高可读性，允许保留更高对比度，视为功能性例外。
- `src/aica/feedback_panel.py` 属于较早期 QWidget 风格，不作为新界面的主参考。后续若改版，应向本规范靠拢。

## 设计定位

AICA 不是通用后台，也不是营销感很强的 SaaS 页面。当前 UI 的正确方向是：

- Windows 桌面工具感
- 温暖、克制、安静
- 像纸张和工作台，不像控制台和大屏
- 强调内容整理，不强调装饰炫技
- 通过圆角、留白、浅层级和轻对比建立秩序

可概括为一句话：

> 暖灰纸感外壳下的轻量任务工作台。

## 核心原则

### 1. 外壳暖，内容清

- 大面使用暖灰、米白、纸感底色。
- 内容层使用更浅的卡片和字段底色。
- 不使用纯黑大块，不使用高饱和大面积底色。

### 2. 层级靠留白和圆角，不靠重阴影

- 当前主线 UI 基本不依赖厚重阴影。
- 层级主要通过不同明度的面、边框和圆角建立。
- 新页面默认不要引入玻璃拟态、发光、夸张阴影。

### 3. 文本优先，图形退后

- 这是一个高频操作工具，不是展示型产品。
- 标题、说明、标签、状态的排布必须先于图标风格考虑。
- 没有必要不要堆图标、插画、横幅、空态大图。

### 4. 一个页面只允许一个主强调色

- 管理/配置场景以绿为主。
- 任务/结果/详情场景以蓝为主。
- 同一屏内不要让绿色、蓝色、橙色同时争主角。

### 5. 轻交互，不做花哨动效

- 当前产品是桌面效率工具，交互反馈应短、轻、直接。
- 主要依赖 hover、focus、pressed、selected 的色彩变化。
- 非必要不要加入长时动画、弹跳、渐变流光。

## 视觉关键词

- Warm
- Quiet
- Rounded
- Paper-like
- Practical
- Structured
- Desktop-native

反向关键词：

- 赛博
- 炫彩
- 紫色渐变
- 标准后台模板感
- 强玻璃感
- 大面积深色模式

## 设计 Token

### 色彩

#### 基础中性色

用于外壳、卡片、字段和弱边界。

| 角色 | 推荐值 | 用途 |
| --- | --- | --- |
| Shell Background | `#F6F2EA` / `#F1F0EC` | 窗口外壳、大面容器 |
| Primary Panel | `#FCF9F3` / `#F7F6F2` / `#F7F7F4` | 主内容卡片 |
| Secondary Panel | `#F3EEE5` / `#F8F2E8` / `#F1EBE0` | 侧栏、次级分组 |
| Field Background | `#FFFEFC` / `#FCFBF8` | 输入框、胶囊标签、次级行动底色 |
| Border Line | `#E7DDCF` / `#E7E4DD` / `#E4DFD4` | 细边框、分隔线 |

约束：

- 大面积背景避免纯白 `#FFFFFF`。
- `#FFFFFF` 只用于小面积字段内部、按钮局部或高亮卡片。
- 边框只能做弱提示，不能做强框线 UI。

#### 文本色

| 角色 | 推荐值 | 用途 |
| --- | --- | --- |
| Title Ink | `#18202E` | 页面标题、卡片标题、主要信息 |
| Body Ink | `#4A5565` | 正文、说明、输入内容 |
| Label Ink | `#7C8795` / `#9AA4B3` | 字段标签、辅助标题 |
| Muted Ink | `#A9B1BD` / `#B3BBC8` | 占位符、弱提示、次级元数据 |

约束：

- 不要使用纯黑 `#000000` 作为主文字。
- 页面内至少保留三层文本层级：标题、正文、弱提示。

#### 强调色

当前系统是双主轴，但不是双主色混用。

| 场景 | 推荐值 | 用途 |
| --- | --- | --- |
| 管理态 Accent | `#3E7B67` | 控制面板、导航选中、保存动作 |
| 管理态 Soft Accent | `#D8EAE2` | 选中背景、弱提示底 |
| 工作态 Accent | `#3D7CFF` / `#5E8CFF` | 待办、结果、详情、焦点输入 |
| 工作态 Soft Accent | `#EEF4FF` | 蓝色高亮背景、投放态、附件预览 |
| Success | `#17663A` | 成功状态 |
| Error | `#B42318` / `#E35B66` | 错误、危险、删除 |
| Warm Hint | `#B7793F` / `#C77B48` | 反馈修正、提醒类弱强调 |

规则：

- 控制面板类页面默认走绿色系统。
- 结果确认、待办详情、焦点输入默认走蓝色系统。
- 警示色只用于状态和局部动作，不能拿来做主题色。

### 字体

#### 默认字体

- 新 QML 界面默认使用 `Microsoft YaHei UI`
- Windows QWidget 兼容层可用 `Segoe UI Variable Text`, `Microsoft YaHei UI`, `sans-serif`

#### 字号层级

| 层级 | 推荐值 |
| --- | --- |
| 页面标题 | `20px / 700` |
| 面板标题 | `17-18px / 600` |
| 分区标题 | `14-15px / 600-700` |
| 正文输入 | `12-13px / 400` |
| 标签说明 | `11-12px / 500-600` |
| 胶囊/元信息 | `10-11px / 500-600` |

规则：

- 标题只用字重拉层级，不用夸张字号。
- 中文界面避免过细字重。
- 多行说明文字优先通过宽度和换行解决，不靠缩小字号硬塞。

### 圆角

当前 UI 的圆角是核心识别特征，必须统一。

| 组件层级 | 推荐值 |
| --- | --- |
| 窗口外壳 | `26-30` |
| 大卡片 / Section Card | `24-26` |
| 中卡片 / Footer Bar / 大输入区 | `18-20` |
| 标准输入框 / 标准按钮 / 字段卡 | `16` |
| Meta Chip / 小卡片 | `12-14` |
| 圆形胶囊按钮 | `999` |

规则：

- 新页面不要回退到 6、8、10 这类偏硬的 web 默认圆角。
- 同一页面最多使用 3 档主要圆角，不要到处散落不同数值。

### 间距

| 场景 | 推荐值 |
| --- | --- |
| 窗口外边距 | `16-24` |
| 卡片内边距 | `14-20` |
| 分区间距 | `12-18` |
| 行内小间距 | `6-10` |
| 表单双列间距 | `12-14` |

规则：

- 用呼吸感建立高级感，不要做高密度后台表单。
- 一屏里宁可少放一点，也不要把控件挤满。

## 布局规范

### 1. 根结构

推荐结构：

1. 透明根容器
2. 一层圆角外壳
3. 标题区
4. 主内容区
5. 需要时增加底部状态/动作条

不要：

- 在根层直接堆多个无关系的大块矩形
- 一屏出现多个竞争性的主容器

### 2. 标题区

标题区应保持固定节奏：

- 左侧是明确的页面标题
- 右侧是文本型操作，如关闭、保存、导出
- 标题区高度保持在 `42-54`
- 操作项优先文字，不必每个动作都做成高饱和按钮

### 3. 侧栏导航

控制面板已形成明确模式，后续沿用：

- 左侧为圆角卡片侧栏
- 纵向堆叠导航项
- 当前项通过浅色底和强调色文字高亮
- 不使用下划线 tab、不使用复杂树形菜单

### 4. 表单区

- 管理型页面优先双列表单
- 信息展示优先“标签在上、值在下”的字段卡
- 输入框高度优先 `44`
- 只读字段和可编辑字段在造型上保持一致，通过交互状态区分

### 5. 列表区

- 列表项是轻卡片，不是表格行
- 选中态使用浅底色和轻微字重变化
- 滚动条尽量细、弱、贴边

## 组件规范

### 按钮

#### Primary Button

- 用于当前页面唯一主动作
- 高度建议 `38`
- 圆角 `16`
- 填充使用当前页面主强调色
- 文字白色

限制：

- 一个主要区域里通常只保留一个 primary
- 不要把“保存”“关闭”“取消”“删除”全做成高亮实心按钮

#### Plain / Secondary Button

- 低饱和浅底或透明底
- 边框很轻
- hover 只做轻微底色变化

#### 文本动作

- 适用于标题栏右侧的关闭、保存、导出
- 不要强行加边框和图标
- 通过颜色和 hover 提供可点击性

### 输入框

- 标准输入框使用浅暖白底
- 焦点态只改边框色，不改大面积背景
- 文本和占位符都保持克制
- 多行输入延续同一圆角和边框逻辑

禁止：

- 焦点态出现夸张蓝光
- 输入框做成纯白硬边 web 默认风格

### Chip / Tag / Meta

- 高度建议 `24`
- 圆角建议 `13`
- 用于场景、模型、耗时、状态等元信息
- 颜色要浅，不要喧宾夺主

### 状态消息

- 成功和错误都使用浅底色卡片承载
- 不要只用纯红/纯绿文字裸奔
- 成功/错误区块要和普通卡片保持同样圆角体系

### 窗口控制按钮

- 使用小尺寸圆角按钮
- hover 采用暖色过渡
- 关闭按钮可以偏暖红，但饱和度仍然要克制

## 交互规范

### Hover

- 轻微变色即可
- 不需要大幅阴影抬升
- 鼠标反馈要稳，不要闪

### Focus

- 只强化边框或选中底色
- 颜色变化要和当前页面 accent 一致

### Selected

- 选中优先使用 `Soft Accent` 背景
- 文字可适度加重
- 不要同时加粗边框、高饱和底、强阴影

### Drag / 浮层

- 可拖拽区域要自然融入标题区
- 浮层的存在感来自轮廓和背景，不来自厚投影

### 动效

- 默认静态
- 若确实需要，时长控制在 `120ms-180ms`
- 只允许用于 hover、focus、展开收起

## 新增 UI 的允许变化范围

允许变化：

- 在同一暖灰纸感体系内微调面色
- 根据场景在绿色系统和蓝色系统中二选一
- 在 `16 / 18 / 24 / 30` 圆角尺度内调整
- 根据内容密度微调 `12-24` 的留白

不允许变化：

- 改成纯白蓝边后台模板风
- 改成深色主题或霓虹科技风
- 使用紫色渐变、玻璃感、重阴影、大量图标
- 大量使用直角矩形和硬分割线
- 同一页面混用两套完全不同的色彩语言

## 新页面落地建议

新增 QML 页面时，优先沿用以下命名和结构习惯：

- `shellBg`
- `panelBg`
- `panelAltBg`
- `panelLine`
- `fieldBg`
- `fieldLine`
- `titleInk`
- `bodyInk`
- `labelInk`
- `mutedInk`
- `accent`
- `accentSoft`
- `uiFont`

建议后续在合适时机抽出统一 `Theme` / `DesignTokens`，但在未抽离前，新增页面也必须先遵守这套命名与取值区间。

## 例外说明

### 截图工具栏

`toolbar.py` 悬浮在真实截图上方，因此可读性优先于纸感：

- 允许更白的底
- 允许更明确的阴影
- 允许更直接的蓝色主按钮

但仍应避免：

- 过重阴影
- 过多颜色
- 与主产品完全脱节的互联网后台按钮风

### 历史 QWidget 页面

像 `feedback_panel.py` 这类旧界面，不要求一次性重做，但后续修改应逐步迁移到：

- 更暖的中性色
- 更大的圆角
- 更轻的边框
- 更少的企业后台感

## 新增 UI 自检清单

提交前至少自查以下问题：

1. 这个页面看起来像 AICA，还是像通用后台模板？
2. 当前页面是否只使用了一个主强调色？
3. 背景是否保持暖灰纸感，而不是纯白或深色？
4. 圆角是否仍然落在现有体系里，而不是突然变硬？
5. 层级是否主要靠留白、面色和文字，而不是靠阴影堆出来？
6. 标题、正文、标签、弱提示是否有清晰层级？
7. 按钮是否克制，是否只保留一个真正的主动作？
8. 输入框、卡片、列表项是否仍然属于同一家族？
9. 页面是否避免了紫色渐变、玻璃感、炫彩图标和大屏风？
10. 如果把它放进现有控制面板/结果面板旁边，会不会显得像另一个产品？

如果第 10 条答案不是“不会”，就说明设计已经跑偏。
