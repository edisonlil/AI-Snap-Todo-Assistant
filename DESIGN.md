# AICA UI 设计规范

本文档用于沉淀 AICA 当前桌面端前端界面的统一视觉规范，覆盖待办窗口体系、截图工具栏、客户端控制面板及其子页面。

这不是组件库实现文档，也不是某一页的视觉稿说明，而是后续抽象组件库、统一配色、统一间距和统一交互时必须遵守的设计规范。

## 1. 适用范围

本规范覆盖以下前端页面与容器：

- 待办浮动面板：`src/aica/qml/TodoPanel.qml`
- 工单待办确认页：`src/aica/qml/ResultDialog.qml`
- 待办详情页：`src/aica/qml/TodoDetailPanel.qml`
- 反馈修正页：`src/aica/qml/FeedbackPanel.qml`
- 阶段总结侧窗：`src/aica/qml/StageSummaryWindow.qml`
- 时间线卡片体系：`src/aica/qml/BaseTimelineCard.qml`、`src/aica/qml/DefaultTimelineCard.qml`、`src/aica/qml/LogAnalysisTaskCard.qml`、`src/aica/qml/LogAnalysisResultCard.qml`
- 客户端控制面板主壳与各分区：`src/aica/qml/ControlPanel.qml`、`src/aica/qml/ProjectsSection.qml`、`src/aica/qml/TicketsSection.qml` 等
- 截图悬浮工具栏：`src/aica/toolbar.py`

## 2. 总体设计定位

AICA 的正确方向不是 Web 后台，不是营销页，也不是重科技感控制台。

它应当是：

- 面向 Windows / macOS 的桌面任务工作台
- 白底、浅灰层级、深灰主强调的轻量专业风格
- 以文字、结构、节奏和信息秩序为主
- 低装饰、低噪音、高可操作性

一句话概括：

> 纯白主壳下的浅灰任务工作台。

## 3. 视觉关键词

正向关键词：

- Clean
- Quiet
- Structured
- Desktop-native
- Lightweight
- White / Gray
- Practical

反向关键词：

- 暖米黄主壳
- 紫色渐变
- 玻璃拟态
- 霓虹科技风
- 大面积蓝底后台模板
- 重阴影、大发光、大插画

## 4. 设计总原则

### 4.1 页面先统一壳体，再统一控件

所有页面优先统一以下三层：

- 主壳：白色
- 内容层：浅灰卡片
- 主动作：深灰按钮

不要先从单个按钮样式开始修，导致整体仍然像不同产品。

### 4.2 层级靠面色和留白，不靠重阴影

当前产品的主要层级来源应当是：

- 白色主壳
- 浅灰卡片 / 字段
- 细边框
- 文字层级
- 圆角尺度

阴影只允许少量出现在悬浮工具栏这类特殊场景。

### 4.3 主强调色只有一个

默认主强调色是深灰，而不是蓝色。

- 主动作：深灰 `#2A313F`
- 焦点和系统反馈：少量蓝色
- 成功 / 错误：状态色，不参与页面主题竞争

### 4.4 文本优先于图标

界面里绝大多数操作都以文字按钮、标签、字段卡的可读性为优先。

能用文字说清楚的，不要强行叠图标。

### 4.5 UI 变更只做视觉，不碰逻辑

后续所有页面收敛应遵循：

- 不改 bridge 接口
- 不改 signal / slot
- 不改字段含义
- 不改保存流程
- 不改筛选和行为逻辑

## 5. 统一设计 Token

本节是后续组件库抽象时的基准 token。

### 5.1 中性色体系

#### 主壳 / 内容底色

| Token | 值 | 用途 |
| --- | --- | --- |
| `shellBg` | `#FFFFFF` | 窗口最外层主壳 |
| `panelBg` | `#FFFFFF` | 主内容面板 |
| `panelAltBg` | `#F5F5F5` | 次级大面、导航区、分区容器 |
| `fieldBg` | `#F5F5F5` | 字段卡、输入区、信息卡 |
| `timelineBg` | `#F5F5F5` | 时间线卡片、列表轻卡片 |
| `inputBg` | `#FFFFFF` | 输入框内部编辑区 |

规则：

- 新页面主壳统一使用纯白 `#FFFFFF`
- 浅灰层统一收敛到 `#F5F5F5`
- 不再新增新的暖白、米白、黄白色面值

#### 边框与分割线

| Token | 值 | 用途 |
| --- | --- | --- |
| `panelLine` | `#E5E7EB` | 主边框、导航边线 |
| `fieldLine` | `#E5E7EB` | 字段边框、输入边框 |
| `sectionLine` | `#E5E7EB` | 分区线、头部与内容分割 |
| `pressedBg` | `#E5E7EB` | 按下态浅背景 |
| `hoverBg` | `#F3F4F6` | hover 浅背景 |

规则：

- 所有边框必须是浅灰弱边框
- 不允许出现高饱和描边作为默认常态
- 焦点时可以提升到强调色边框，但不能出现荧光边框

### 5.2 文本色体系

| Token | 值 | 用途 |
| --- | --- | --- |
| `titleInk` | `#18202E` | 页面标题、区块标题、主信息 |
| `bodyInk` | `#4A5565` | 正文、可编辑内容、说明 |
| `labelInk` | `#7C8795` / `#9AA4B3` | 字段标签、弱标题 |
| `mutedInk` | `#A9B1BD` | 占位、提示、时间、说明 |
| `chipInk` | `#344054` / `#5B6574` | 标签、元信息胶囊文字 |

规则：

- 不使用纯黑作为主文案色
- 页面内至少有标题、正文、弱提示三层文字层级

### 5.3 强调色体系

#### 主强调

| Token | 值 | 用途 |
| --- | --- | --- |
| `accent` | `#2A313F` | 主按钮、选中按钮、深色文字动作 |
| `accentSoft` | `#F1F3F6` | 控制面板导航选中底 |
| `accentTint` | `#ECEFF3` | 浅强调底、选中辅助底 |

规则：

- 后续所有管理页、待办页、确认页、反馈页优先统一使用这套深灰主强调
- 深灰主强调应被视为 AICA 当前主品牌色

#### 功能性蓝色

| Token | 值 | 用途 |
| --- | --- | --- |
| Utility Blue | `#1677FF` | toolbar 状态、强焦点、少量工具态反馈 |
| Utility Blue Soft | `rgba(22,119,255,0.08~0.24)` | 工具栏选中态、下拉选中态 |

规则：

- 蓝色只用于工具态和焦点反馈
- 不再作为业务页面的主主题色大面积使用

#### 状态色

| Token | 值 | 用途 |
| --- | --- | --- |
| `successBg` | `#E7F5ED` | 成功状态底 |
| `successInk` | `#17663A` | 成功状态字 |
| `errorBg` | `#FDECEC` | 错误状态底 |
| `errorInk` | `#B42318` | 错误状态字 |
| `dangerInk` | `#E35B66` | 删除、移除等危险动作 |

### 5.4 字体体系

默认字体来源于运行时：

- Windows：`Microsoft YaHei UI`
- macOS：`PingFang SC`
- 等宽内容：Windows 常用 `Consolas` / macOS 常用 `SF Mono` / `Menlo`

建议字号层级：

| 层级 | 建议值 |
| --- | --- |
| 页面主标题 | `18-20 / 600-700` |
| 区块标题 | `14-17 / 600` |
| 标准正文 | `12-13 / 400` |
| 标签文字 | `11-12 / 500-600` |
| Chip / Meta | `10-11 / 500-600` |

规则：

- 中文不使用过细字重
- 标题层级靠字重和留白，不靠极大字号

### 5.5 圆角体系

| 组件层级 | 建议值 |
| --- | --- |
| 窗口外壳 | `24-30` |
| 大内容卡片 | `18-24` |
| 字段卡 / Footer / 输入容器 | `16-18` |
| 小按钮 / 胶囊切换 | `15-16` |
| Chip / 小卡片 | `12-14` |
| 圆形操作按钮 | `999` |

规则：

- 不再回退到偏硬的 6 / 8 圆角
- 同一页面控制在 3 档主要圆角里

### 5.6 间距体系

| 场景 | 建议值 |
| --- | --- |
| 页面外边距 | `16-24` |
| 卡片内边距 | `14-20` |
| 大分区间距 | `12-18` |
| 行内小间距 | `6-10` |
| 双列字段间距 | `12-14` |

## 6. 控件级规范

### 6.1 主按钮

视觉：

- 背景：`#2A313F`
- 文字：`#FFFFFF`
- 圆角：`15-16`
- 高度：`30-38`
- 字重：`700`

使用位置：

- 保存
- 主提交动作
- 当前页唯一主操作

禁止：

- 一个区域出现多个同层级深色主按钮

### 6.2 次按钮

视觉：

- 背景：`#FFFFFF`
- 边框：`#E5E7EB`
- 文字：`#4A5565`
- 圆角：`15-16`

使用位置：

- 关闭
- 导出
- 展开 / 收起
- 次级切换

### 6.3 Ghost / 文本按钮

仅用于：

- 时间线里的低权重文字动作
- 列表项内小操作
- 辅助链路入口

规则：

- 默认不应与主按钮争主视觉
- hover 只做轻微背景变化或字色增强

### 6.4 输入框与编辑区

统一规则：

- 容器底：`#F5F5F5`
- 编辑区底：`#FFFFFF`
- 常态边框：`#E5E7EB`
- 焦点边框：主强调色或工具蓝

说明：

- 业务详情页、确认页、反馈页都应使用同一套输入层级
- 输入焦点不应触发大面积底色跳变

### 6.5 字段卡

字段卡统一为：

- 标签在上、值在下
- 背景 `#F5F5F5`
- 圆角 `16`
- 标签用 `labelInk`
- 值用 `titleInk` 或 `bodyInk`

### 6.6 Chip / 元信息胶囊

视觉：

- 高度：约 `24`
- 圆角：约 `13`
- 背景：`#F5F5F5`
- 文字：`chipInk`

使用位置：

- 场景
- 模型
- 耗时
- 状态简要标签

### 6.7 状态卡与危险动作

状态卡必须使用浅底：

- 成功：浅绿底 + 深绿字
- 错误：浅红底 + 深红字

危险动作：

- 使用 `dangerInk`
- 不做高饱和填充大按钮，除非用户明确确认流程要求

## 7. 页面级规范

### 7.1 控制面板体系

覆盖：

- `ControlPanel.qml`
- `ProjectsSection.qml`
- `TicketsSection.qml`
- 各类 ControlPanel 子组件

结构特征：

- 白色主壳
- 左侧浅灰导航区域
- 右侧白色内容区
- 导航选中为深灰文字 + 浅灰底
- 主操作统一用深灰主按钮

控制面板是整个客户端设计语言的主参考页。

后续所有新页面，优先向控制面板体系靠拢。

### 7.2 待办浮动面板体系

覆盖：

- `TodoPanel.qml`

定位：

- 极简浮动信息面板
- 不做复杂分区
- 默认白色壳体
- 操作尽量胶囊化

规范要求：

- 外壳白色
- 头部信息弱化
- 右上角小操作统一胶囊按钮风格
- 选中项使用极浅底和深灰选中点

备注：

- 当前 `TodoPanel` 仍残留少量偏暖色值，如 `#FFFDFC` / `#ECE7DE`
- 后续若继续收敛，应全部归并到 `#FFFFFF` / `#F5F5F5` / `#E5E7EB`

### 7.3 工单待办确认页

覆盖：

- `ResultDialog.qml`

规范：

- 页面主壳纯白
- 标签、字段卡、结论区域、底部栏统一浅灰
- 右上角动作区为按钮式，不再是纯文字入口
- 主操作使用深灰实心按钮

这是待办业务页面视觉收敛的基准页。

### 7.4 待办详情页

覆盖：

- `TodoDetailPanel.qml`
- 时间线内容区
- 草稿输入区
- 附件区

规范：

- 与工单待办确认页保持同一套主壳和字段色
- 头部动作区统一按钮化
- 字段卡 / 详情块 / 时间线块统一用浅灰卡
- 添加时间线的输入区应看起来像“工作区”，不是 web 表单 textarea
- 阶段总结和展开收起都应作为次按钮存在

当前已按这个方向收敛，但时间线内部子卡片仍有继续统一空间。

### 7.5 反馈修正页

覆盖：

- `FeedbackPanel.qml`

规范：

- 纯白背景
- 内容卡片和底部栏为浅灰
- 保存按钮使用控制面板主按钮样式
- 页面信息结构以“标题 + 元信息 + 大编辑区 + 辅助说明 + 底部动作”组织

### 7.6 阶段总结侧窗

覆盖：

- `StageSummaryWindow.qml`

定位：

- 是待办详情的邻接工作区，不是独立品牌页

规范目标：

- 应逐步向待办详情页和确认页的白灰体系靠拢
- 主按钮仍可保留深色实心按钮
- 当前偏蓝的内容盒与 chips 可保留过渡态，但后续不应继续扩散

备注：

- 这是当前 UI 体系里仍然存在相对独立视觉语言的页面之一
- 后续如统一组件库，应优先收敛此页

### 7.7 时间线卡片体系

覆盖：

- `BaseTimelineCard.qml`
- `DefaultTimelineCard.qml`
- `LogAnalysisTaskCard.qml`
- `LogAnalysisResultCard.qml`

规范目标：

- 卡片底色统一为浅灰系，而不是蓝底或黄底
- 删除、上传、复制、粘贴等动作默认以文字按钮存在
- 状态 pill 使用浅色状态底，不使用强实心块
- 元信息顺序优先于装饰图形

### 7.8 截图工具栏

覆盖：

- `toolbar.py`

这是唯一明确允许“更强对比度”的前端区域。

原因：

- 它浮在真实截图之上
- 背后背景不可控
- 可读性优先于页面一致性

规范：

- 主壳使用半透明白底
- 允许轻阴影
- 主按钮为深灰
- 工具态选中和状态标签允许使用蓝色
- 小按钮保留 Ghost 风格

注意：

- 工具栏允许存在功能性蓝色
- 但它不应反向影响业务页配色

## 8. 当前统一配色方案

后续页面统一时优先遵循以下配色：

### 8.1 主业务页面配色

- 主壳：`#FFFFFF`
- 浅灰内容层：`#F5F5F5`
- 细边框：`#E5E7EB`
- 主标题：`#18202E`
- 正文：`#4A5565`
- 标签：`#7C8795` 或 `#9AA4B3`
- 弱提示：`#A9B1BD`
- 主强调：`#2A313F`
- 主强调浅底：`#ECEFF3` / `#F1F3F6`

### 8.2 工具栏配色

- 半透明白底：`rgba(255,255,255,244)`
- 主按钮：`#2A313F`
- 主按钮 hover：`#394152`
- 主按钮 pressed：`#1F2531`
- 次按钮底：`#F3F4F6`
- 焦点 / 工具蓝：`#1677FF`

### 8.3 状态配色

- Success Bg：`#E7F5ED`
- Success Ink：`#17663A`
- Error Bg：`#FDECEC`
- Error Ink：`#B42318`
- Danger Ink：`#E35B66`

## 9. 组件库预备命名规范

虽然现在先不抽组件库，但后续抽离时，设计 token 和 props 命名建议保持如下：

- `shellBg`
- `panelBg`
- `panelAltBg`
- `panelLine`
- `sectionLine`
- `fieldBg`
- `fieldLine`
- `timelineBg`
- `inputBg`
- `titleInk`
- `bodyInk`
- `labelInk`
- `mutedInk`
- `chipInk`
- `accent`
- `accentSoft`
- `accentTint`
- `successBg`
- `successInk`
- `errorBg`
- `errorInk`
- `dangerInk`
- `uiFont`

按钮命名建议：

- `PrimaryButton`
- `SecondaryButton`
- `GhostButton`
- `IconGhostButton`
- `TextAction`

容器命名建议：

- `PageShell`
- `SectionCard`
- `FieldCard`
- `MetaChip`
- `FooterBar`
- `InlineStatusPill`

## 10. 后续 UI 收敛优先级

如果后续继续统一界面，建议按这个顺序推进：

1. 统一 `TodoPanel.qml` 的剩余暖色值
2. 统一 `StageSummaryWindow.qml` 的蓝色内容盒和按钮体系
3. 统一时间线子卡片内部动作文字的样式族
4. 抽离 `PrimaryButton / SecondaryButton / FieldCard / MetaChip` 的通用组件
5. 抽离共享 Theme / Token 文件

## 11. 新页面自检清单

新增或调整页面时，至少逐项自查：

1. 它是否属于白底 + 浅灰卡片 + 深灰主强调体系？
2. 页面里是否只有一个主强调色？
3. 是否仍然以文字信息和结构为主，而不是图标和装饰为主？
4. 主按钮是否只有一个视觉主角？
5. 输入区、字段卡、标签、底部栏是否属于同一家族？
6. 圆角是否仍然在 `12 / 16 / 18 / 24 / 30` 这些尺度中？
7. 边框是否都落在 `#E5E7EB` 体系附近？
8. 页面是否避免了新的暖米黄、亮蓝主底、紫色或炫彩渐变？
9. 这个页面放到控制面板、确认页、待办详情页旁边时，会不会像另一个产品？
10. 如果准备抽组件，它的视觉 token 是否已经能映射到本规范中的命名？

如果第 9 条答案不是“不会”，说明这个页面还没有收敛到 AICA 的统一设计体系里。
